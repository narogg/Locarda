<!--<h1><span class="label label-danger">@ Mjesta s LocalZoo-om</span></h1><br>-->
<script>
$(document).ready(function () {
    // will call refreshPartial every 3 seconds
    setInterval(refreshClientsPartial, 3000)

});

// calls action refreshing the partial
function refreshClientsPartial() {
  $.ajax({
    url: "places/refresh"
 })
}
</script>

<% @places.each do |place| %>

<div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">
					<!--<span class="label label-default"></span>-->
                        <h3><strong><%= place.name %>&nbsp;</strong><td><%= place.description %></td></h3></h3>
                </div>
			<div class="panel-body">
			<table class ="table">
			<tr>
			<td>
			<%= image_tag(place.image_url, :class => 'img-rounded img-responsive') %></td>			
			<td><h4><strong><%= place.rewards %></h4>&nbsp; 
			
			<%# If there is no card, user has no points, so display zero - spaghetti code for sure%>
			<%# if cart = Cart.find_by(user_id: current_user.id, place_id: place.id).present?%>
			<%#cart = Cart.find_by(user_id: current_user.id, place_id: place.id)%>
			<%#=cart.points%>
			<%#else%>			
			<%# end %>
			</td>			
			<td><%#= button_to 'Check in', carts_path(:place_id => place), class: "btn btn-info pull-right" %></td>
			
			
			
			
			<td><%#= link_to 'Show', place %></td>
			<td><%= link_to 'Edit', edit_place_path(place),class: "btn btn-default pull-right" %></td>			
			<td><%#= link_to 'Destroy', place, method: :delete, data: { confirm: 'Are you sure?' } %></td>
			</tr>
			
								
			<tr>			
			<td><footer class="text-muted"><%= place.full_name %> </footer> </td>			
			</tr>
			</table>
			</div>
			
	</div>
<% end %>
	<div id ="clients_table">
	  <%= render 'clients' %>
    </div>


<br>

<%= link_to 'Novi lokal', new_place_path, :class =>'btn btn-default' %>
